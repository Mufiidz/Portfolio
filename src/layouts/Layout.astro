---
import Footer from "../components/Footer.astro";
import NavBar from "../components/Navbar.astro";
import background from "../assets/background.png";

const redirectUrl = 'https://mufiidz.github.io/';
const delay = 3;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <!-- <meta http-equiv="refresh" content={`${delay}; url=${redirectUrl}`}/> -->
    <title>Rifqi Mufidianto Portofolio</title>
  </head>
  <NavBar />
  <body class="w-screen h-screen">
    <div class="relative">
      <div>
        <img
          id="background"
          src={background.src}
          alt="background"
          fetchpriority="high"
        />
        <div>
          <slot />
        </div>
      </div>
    </div>
  </body>
  <Footer />
</html>

<style is:global>
  @tailwind base;
  @tailwind components;
  @tailwind utilities;

  html,
  body {
    margin: 0;
    color: #fff;
  }
  html.dark {
    background-color: #000000;
    color: #fff;
  }
  .dark .nav-links a {
    color: #fff;
  }
  #background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    pointer-events: none;
    user-select: none;
  }
  .dark #background {
    filter: brightness(50%) contrast(120%);
  }
</style>
